version: '3.5'

services:
  codecept:
    image: codeception/codeception
    command: run --steps
    volumes:
      - ../../services/browser-tests:/project
      - ../../services/browser-tests/tests:/tests
      - ../../services/browser-tests/codeception.yml:/codeception.yml

networks:
  default:
    external:
      name: dockercomposetestnetwork_default